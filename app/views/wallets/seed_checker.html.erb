<script>
    $( document ).ready(function() {
        var spinner = $("#loading");
        spinner.spin({
            color: '#777',
            top: '40%',
            length: 20,
            width: 15,
            radius: 30
        });
        spinner.spin(false);
        spinner.hide();
    });
</script>

<div id="seed_checker_notifications"></div>

<div class="form-group string">
  <label class="control-label string required" for="seed"><abbr title="required">*</abbr> Seed</label>
  <input class="form-control string required" required="required" aria-required="true" type="text" name="wallet[username]" id="seed_input" />
  <label class="control-label string required" for="seed"><abbr title="required">*</abbr> Security</label>
  <select id="sec_level">
    <option value="1">1</option>
    <option value="2" selected>2</option>
    <option value="3">3</option>
  </select>
</div>
<button class="ladda-button btn btn-primary btn" data-style="expand-right" id="login_btn" onclick="check_seed()">
  <span class="ladda-label">Run</span>
</button>

<div id="loading" ><p class="text-muted">Loading addresses...</p></div>
<div id="seed_checker_result" style="padding-top: 1em;"></div>
<div id="addresses_div" hidden="hidden">
  <table class="table fixed-table">
    <thead>
    <tr>
      <th>Address</th>
      <th style="width: 6em">Balance</th>
    </tr>
    </thead>
    <tbody id="address_list">
    </tbody>
  </table>
</div>